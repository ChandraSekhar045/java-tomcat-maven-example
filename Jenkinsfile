pipeline {
    agent any
    stages {
        stage ('Initialize') {
            steps {
               sh ***
               echo "PATH = ${PATH}"
               echo "M2_HOME = $(M2_HOME)"
               ***

                /*For Mac & Linux machine */
               // sh  'mvn clean package'
            }
        }

        stage ('Build'){
            steps{

                echo "Hello World"

            }
        }
    }
}
